# Dependencia da placa. Na Raspberry Pi do laboratorio a RPi.GPIO ja vem no
# Python do sistema (conferido: 0.7.1a4), entao nao e preciso venv nem pip.
# O marcador abaixo evita que ela tente instalar na maquina de desenvolvimento,
# onde o modo --simulado nao precisa dela.
RPi.GPIO>=0.7.1; platform_machine == "armv7l" or platform_machine == "aarch64"

# Apenas para a bateria de testes automatizados.
pytest>=7.0
